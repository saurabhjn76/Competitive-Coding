#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
                int i,n,t;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        {
        scanf("%d",&t);
        printf("%d\n",t+1);
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
